[
    {
        "release/tables": [
            {
                "method": "getTableTreesForRelease",
                "description": "Get release table trees for a given release. You can go directly to the tree structure by passing the appropriate element_id. You may also use a drill-down approach to start at the root (top most) element by leaving the element_id off.",
                "request-type": "GET",
                "parameters": {
                    "release_id": {
                        "required": true,
                        "type": "integer",
                        "default": 0,
                        "description": "The id for a release."
                    },
                    "element_id": {
                        "required": false,
                        "type": "integer",
                        "default": 1000,
                        "description": "The release table element id you would like to retrieve.",
                        "restrictions": "When the parameter is not passed, the root(top most) element for the release is given."
                    },
                    "include_observation_values": {
                        "required": false,
                        "type": "string",
                        "default": false,
                        "description": "A flag to indicate that observations need to be returned. Observation value and date will only be returned for a series type element.",
                        "restrictions": "One of the following strings: 'true', 'false'."
                    },
                    "observation_date": {
                        "required": false,
                        "type": "date",
                        "default": "9999-12-31 (latest available)",
                        "description": "The observation date to be included with the returned release table.",
                        "restrictions": "YYYY-MM-DD formatted string"
                    }
                }
            }
        ]
    }
]